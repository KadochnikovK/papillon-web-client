import { createSlice, createAsyncThunk } from "@reduxjs/toolkit";

const settingsSlice = createSlice({
  name: "settings",
  initialState: {
    data: {

    },
    error: null,
    loading: false,
  },
});




